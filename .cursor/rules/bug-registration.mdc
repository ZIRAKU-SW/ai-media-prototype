---
description: バグ発見・修正時の SQLite 登録手順（過去トラブル参照用）
alwaysApply: true
---

# バグ登録ルール（全エージェント必須）

**どのエージェントでも**、バグ・トラブルを直したら **コード修正 + SQLite 登録 + 仕様更新** までセットで行う。  
「直したから終わり」は不可。`git push` の前に台帳登録まで完了すること。

## いつ登録するか（必ず登録）

- 本番またはモバイルで再現した表示崩れ・データ不整合
- 3テーマのうち1つだけ直って他が残る類の不具合
- 同じ症状が再発しそうな CSS / レイアウト問題
- **過去セッションで直したが未登録のトラブル**（会話履歴・コミットログを見て漏れを埋める）
- **デプロイ失敗・ビルドエラー**（原因と対策が分かれば登録する）
- ユーザーから「バグ」「崩れ」「直して」と言われて対応したもの

## 登録手順

1. **修正を実装**し、原因と対策を1〜2文で整理
2. **登録コマンド**（`platform_meta/seed.py`）:

```bash
python3 platform_meta/seed.py --register-bug \
  --title "一行要約" \
  --symptom "ユーザーが見た現象" \
  --root-cause "技術的原因" \
  --fix "変更ファイルと内容" \
  --themes notion,wired,zapier \
  --status fixed \
  --commit <git-hash>
```

3. **エクスポート確認**: `data/platform-bugs.json` が更新されていること
4. **仕様更新**: `doc/AGENT_SPEC.md` §6 に1行追記
5. **コミットに含める**: `data/platform.db` と `data/platform-bugs.json` を一緒に commit

## 参照

- 一覧: `/admin/operations`（運用タブ）
- ローカル: `sqlite3 data/platform.db "SELECT * FROM bugs ORDER BY id DESC;"`
- 既知バグの初期データ: `platform_meta/seed.py` の `BUG_ENTRIES`

## 作業開始時・終了時のチェック

1. **開始時**: `data/platform-bugs.json` または SQLite で同種バグを検索
2. **修正後**: 上記登録手順を実行（省略禁止）
3. **終了時**: 今回のセッションで直したものがすべて台帳に載っているか確認

## 再発防止

修正前に `BUG_ENTRIES` / SQLite を検索し、同種の過去事例がないか確認する。  
台帳にない過去トラブルは再発時に同じ調査を繰り返す原因になる。
